# 可选模型适配

仅在用户指定模型或要求跨模型转译时使用。默认保持模型中立，不加载模型名录。

1. 保留同一场景事实、人数、身份锚点、构图、光线和避错项。
2. 只根据目标工具已核实的能力调整提示词长度、画幅参数、参考图与局部编辑传递方式。
3. 将工具参数放在提示词外。未核实的参数、权重语法或版本能力不编造；保留自然语言并说明待确认项。
4. 不把适配等同于画质提升，不承诺不同模型输出一致。

交付一个适配提示词，必要时附参数和仍需人工核对的差异；没有实际生成时只标为文本适配完成。
