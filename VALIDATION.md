# 压力测试结果

- 日期：2026-08-28 至 2026-09-02
- 版本：1.2.0
- 方式：主流程 fallback 自测 + 实际 GPT Image 2 / MiniMax H3 产物验收。项目指令禁止开启子线程，因此未做独立 sub-agent 盲测；可信度低于真正盲测。
- 结果：11/11 路由与规则检查符合预期，诱饵 2/2 通过。本轮冻结六场景的最终版本计数为 5/6 首轮通过，唯一剩余首轮失败 A05 经一次单变量修复通过；A02 的服装伪徽记先建立 RED、最小修订后从零重测通过，并由未见轮渡机修场景做前向验证。H3 已落地静帧的 4.458 秒稳定保持测试通过。

## 逐条结论

| 用例 | 触发判断 | 关键动作 | 结果 |
|---|---|---|---|
| should-trigger-01 | 触发 | 提示词契约 + 信息层级 + 可动层 | 通过 |
| should-trigger-02 | 触发 | 单变量修复信息层级 | 通过 |
| should-trigger-03 | 触发 | continuity lock + 单一计划变化 | 通过 |
| should-trigger-04 | 触发 | 修正曝光信息取舍冲突 | 通过 |
| should-trigger-05 | 触发 | 构图留桩 + 可见接触 + 一次重测停止条件 | 通过 |
| should-trigger-06 | 触发 | 终态/接触时限/保持分别验收 + 首尾帧后二级停止条件 | 通过 |
| should-trigger-07 | 触发 | 制服/工装袖面和胸袋的正面空白表面定义 + 连续性锁定 | 通过 |
| should-not-trigger-01 | 不触发 | 转 API/技术文档能力 | 通过 |
| should-not-trigger-02 | 不抢 H3 字段职责 | 转 `h3-prompt-writing` | 通过 |
| edge-01 | 条件触发 | 只处理底图，文字另行处理 | 通过 |
| edge-02 | 不触发 | 保持电商摄影任务边界 | 通过 |

## H3 实测回归

- 图书馆低幅度运动：`有条件通过`。主体和场景稳定，但微动作可读性较弱。
- 黄狮初测：`不通过`。推近导致钢桩与落点出画，接触不可验证。
- 黄狮聚焦修复重测：`不通过`。钢桩刚性和后段稳定姿态改善，但 3.0–4.0 秒仍推近裁切，指定左侧大平台落地未被证明。
- 黄狮首尾帧：`不通过`。固定宽景和末段双桩姿态明显改善，但 3.45–4.50 秒后舞者的双脚承重仍未同时可见，末段只接近尾帧且没有完整证明两秒稳定保持。
- 黄狮已落桩稳定保持：`通过`。从已通过的落桩静帧开始，4.458 秒内四只鞋持续分离并压在两根钢桩圆盘上，固定宽景未推近或裁掉接触，钢桩保持刚性；该结果不代表高速跳跃过程已经解决。
- 规则因此新增四个结构槽位：留画锚点、镜头护栏、接触时限、稳定保持；同一缺陷只允许一次聚焦重测，仍失败即记录当前模型边界。
- 首尾帧实测进一步新增独立判定：末帧相似不能替代规定时限内的全部接触点和完整保持；专用首尾帧做一次仍失败时停止当前模型重试，改用落地静帧剪辑点、拆镜头或更换模型/工作流。

## 剩余风险

触发逻辑、服装空白表面控制、单帧停止条件与首尾帧后二级停止条件已覆盖。真正独立盲测仍可在允许独立线程的环境中补做。不能宣称 H3 已解决精确高速落桩；当前完成状态是“Skill 已能生成合格首尾静帧、分别验收终态与过程、识别模型边界并切换到已落地静帧的稳定保持路线”，不是“当前 H3 工作流已经通过高速四足落点控制”。

## v1.1 小红书产品化回归（2026-09-01）

- 结构契约：初测暴露 15 项产品化缺失；补齐“镜叙”品牌入口、三种产品模式、3:4 / 9:16 路由、安全裁切、专用工作流、评分表和 10 条测试集后，`tests/xhs-productization.test.ps1` 全通过。
- 生成实测：4 个代表场景 4/4 首轮通过，超过“至少 3/4”的门槛；包括黄色南狮花桩、夜班修车、醒狮修补和末班公交。
- 移动端检查：所有场景均制作 360 像素缩略图；两个 9:16 视频首帧经居中 3:4 裁切后仍保留人物、动作和关键物件关系。
- 连续性检查：醒狮修补相邻帧保持人物、靛蓝工装、旧狮头、材料磨损和光向，只改变景别与下一动作拍点，判定通过。
- 路由审查：10 个小红书用例 10/10 符合预期，其中 8 个进入单帧、连帧、诊断或文字边界流程，2 个正确转出到文案运营或电商摄影；该结果是规则审查，不是独立模型盲测。
- 商业包装：新增 `GO-TO-MARKET.md`，定义 Lite、Pro、Studio 三层、价格假设和真实用户激活/复用/付费验证门槛；不得把生成测试外推为市场验证。
- 正式报告：`research/gpt-image-2-cinematic-prompting/generated-tests/2026-09-01-v11-xhs/xhs-product-test-report.md`。
- 边界：本轮没有真实发布、流量、用户付费、版权或平台审核证据；未将视觉通过外推为“爆款”或商业验收。

## v1.2 自动画幅与多角色组图回归（2026-09-02）

- RED：旧规则把小红书图文首图写死为 3:4、视频源帧写死为 9:16，且没有总角色数/单帧人数与世界连续性/身份连续性的分离契约；新增契约测试后按预期失败 7 项与 4 项。
- GREEN：改为发布硬规格 → 运动轴 → 人物关系 → 空间尺度 → 派生裁切的自动画幅决策；新增角色分组契约与世界-only 参考提示，`tests/xhs-productization.test.ps1` 恢复全通过。
- 生成实测：《八客渡潮》按横向渡海、两人并列和落点同框选择统一 16:9，而不是竖图；四张均为 1672 × 941，覆盖八位去重人物。
- 首轮结果：3/4 首轮通过；第四张仅“张果老顺骑”失败，经一次单变量方向修复后通过，最终为 `PASS AFTER ONE REPAIR`。
- 移动端检查：四张 480 × 270 缩略图仍能读取人物组合、动作方向与法器；本轮没有派生竖版需求，因此不强制 3:4 硬裁。
- 正式报告：`output/imagegen/2026-09-02-eight-immortals-crossing/qa-report.md`。

## v1.2 原创异星生态参考转译回归（2026-09-02）

- 输入边界：用户以具体科幻电影作审美参考；生产提示词没有使用电影、导演、角色或物种名，而是转译为巨物尺度、生物发光、生态互联、空中纵深与潮湿材料。
- 原创替换：两名人类生态勘探员、潮汐拱树、六桨潮兽、气囊浮游兽、矿物种子和根槽；主动排除蓝色类人族群、神经辫、骑乘飞行兽、悬浮山、圣树与军事载具。
- 生成实测：《潮汐穹林》四张均为 1672 × 941、16:9；人物、服装、样本筒、斜挎包、世界光色和树木几何保持连续。
- 首轮结果：3/4 首轮通过；第四帧把矿物种子误解为水滴且提前产生波纹，经一次“动作阶段”修复后通过，最终为 `PASS AFTER ONE REPAIR`。
- 结论：现有“参考转译而非直接模仿”不变量在本场景有效，未观察到需要修改 SKILL 的新失败；保留公开发布时的人工相似风险与权利检查。
- 正式报告：`output/imagegen/2026-09-02-tidal-vault-forest/qa-report.md`。
